//#include "vld.h" // No memory leaks DETECTE!
#include "utils.h"
#include "mat.h"

int main() {

	utils::dicShow("mathematics", "Números Irracionais");

	double raiz;
	for(int i=2; i<30; i++)
		if(!(math::_isRoot(2, i, raiz)))
			P_(raiz)

	system("pause");
	return(0);
}